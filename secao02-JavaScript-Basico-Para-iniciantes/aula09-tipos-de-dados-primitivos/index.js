const nome = 'Lucian'; // String
const num1 = 10; // number
const num2 = 10.5 // number
let nomeAluno; // undefined = não aponta pra local algum na memória
const sobrenomeAluno = null; // Nulo -> não aponta  pra local algum na memória
const aprovado = true; // boolean = true ou false (lógico)

console.log(typeof nome, num1)
